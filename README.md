## Animation illustrating the difference between causal and noncausal filtering

![hippo](./filter_animation.gif)
The segment of the input signal used to compute the current output is shown in **bold black**. As illustrated, **causal** filtering relies only on **past** data up to the current time point, whereas **noncausal** filtering also incorporates **future** data.
